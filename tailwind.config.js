/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./*.{html,js}"],  // Adjust the path to reflect where your files are
    theme: {
      extend: {},
    },
    plugins: [],
};